server:
  servlet:
    contextPath: /app
  port: 8080

logging:
  level:
    root: INFO

app:
    params:
      kafka:
        bootstrapAddress: localhost:9092
        topics:
          businessTopic: business-topic

